## Getting started

- Run `uv sync --all-packages --all-extras` to sync all deps
- `uv run fastapi dev projects/api/src/app/main.py` to run FastAPI server
- Can import other backend projects with `from transcribe import ...`
